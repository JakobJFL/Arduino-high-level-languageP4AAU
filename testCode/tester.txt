proc setup() {}

proc loop() {}

proc outer(Num param4) {
	param4 = 5;
}

proc ledHandler(Num param1, Num param2) {
	proc inner(Num param3) {
		param1 = 0;
		param3 = 0;
		inner(5);
		outer(5);
	}
}
	
	